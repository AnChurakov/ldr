@model IEnumerable<ClientManager.Models.InfoProject>
@{
    ViewBag.Title = "Действующие проекты";
}

<h2>@ViewBag.Title</h2>

<div class="col-lg-12 col-md-12">

    @foreach (var project in Model)
    {
        <a href="SelectProject/@project.Id">@project.Name</a><br />
    }

</div>
